SELECT RECIP_FIRST_NAME, RECIP_LAST_NAME, RECIP_DOB, RECIP_AGE, DS1_ADMIN_DATE, DS2_ADMIN_DATE, RESPONSIBLE_ORG, LHJ
FROM CA_VACCINE.PUBLIC.TBL_IMMUN_MONKEYPOX_RECIPIENTS
WHERE LHJ = 'San Diego'
AND contains(responsible_org,'RITE AID')
